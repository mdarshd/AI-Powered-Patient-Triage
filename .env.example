# AWS Lambda Function URL that serves the triage backend.
# Copy this file to .env (or set as an environment variable / Streamlit secret)
# and fill in your actual endpoint. Never commit real values.
LAMBDA_URL=https://your-lambda-function-url.on.aws/
